galaxy_info:
  author: Adrian Reyer <lihas@lihas.de>
  namespace: lihas
  description: Install dehydrated with http-01, test domains against staging and finally deploy them for real
  license: GPL-3.0-or-later
  galaxy_tags: []

  min_ansible_version: 2.9

  platforms:
    - name: Debian
      versions:
        - all

dependencies:
  - name: lihas_apache
    src: https://github.com/LinuxHaus/ansible_lihas_apache.git
    version: origin/master
    when: false
